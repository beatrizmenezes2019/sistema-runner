linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - misspell
    - bodyclose

linters-settings:
  errcheck:
    check-type-assertions: true

issues:
  exclude-rules:
    - path: cmd/
      linters: [errcheck]
