name: Sistema Runner CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-go-apps:
    name: Build CLIs Go
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Plataformas exigidas
        goos: [linux, windows, darwin]
        # Aplicações que queremos compilar
        app: [cli-assinatura, cli-simulador]
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.22'

    - name: Build Multiplataforma
      # O working-directory faz o 'go build' rodar dentro da pasta da aplicação
      working-directory: ./${{ matrix.app }}
      run: |
        BINARY_NAME=${{ matrix.app }}-${{ matrix.goos }}-amd64
        if [ "${{ matrix.goos }}" = "windows" ]; then BINARY_NAME=$BINARY_NAME.exe; fi
        
        # O ponto (.) indica que o main está na raiz da pasta da aplicação ou subpasta cmd
        # Ajuste o caminho abaixo se o seu main.go estiver em cmd/assinatura/
        GOOS=${{ matrix.goos }} GOARCH=amd64 go build -o ../build/$BINARY_NAME ./cmd/assinatura/main.go || go build -o ../build/$BINARY_NAME .

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: binarios-${{ matrix.app }}-${{ matrix.goos }}
        path: build/